type Query {
    photos: [Photo]
}

type Photo {
    id: ID!
    name: String
    path: String
}

type Mutation {
    newPhoto(name: String!) : Photo!
    deletePhoto(id: ID!) : Boolean
    updatePhoto(id:ID!, name: String!) : Photo!
}